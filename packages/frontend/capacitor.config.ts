import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
	appId: 'io.github.hieudoanm.chat',
	appName: '@chat/frontend',
	webDir: 'public',
};

export default config;
